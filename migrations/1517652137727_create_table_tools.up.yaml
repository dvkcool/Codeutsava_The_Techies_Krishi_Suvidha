- args:
    sql: CREATE TABLE "tools"("id" serial NOT NULL, "name" text NOT NULL, "seller"
      text NOT NULL, "price" integer NOT NULL, "image" text NOT NULL, PRIMARY KEY
      ("id") )
  type: run_sql
- args:
    name: tools
  type: add_existing_table_or_view
